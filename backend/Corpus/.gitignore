# Generated benchmark reports (regenerated by `python benchmark.py`).
benchmark/report.md
benchmark/report.json

# The trend/regression history IS tracked (benchmark_history.json at this level)
# — it is the durable record of v0.4 → v0.5 → v0.6 per software.
